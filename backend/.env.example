############################################################
# PusulaFit Backend — Production Ortam Değişkenleri
# Bu dosyayı kopyala: cp .env.example .env
# .env dosyasını asla commit'leme!
############################################################

# ── DATABASE (Supabase PostgreSQL) ───────────────────────
# Supabase: Project Settings > Database > Connection string (URI)
# jdbc: prefix ekleyerek yapıştır, ?sslmode=require ekle:
# Örnek: jdbc:postgresql://db.<ref>.supabase.co:5432/postgres?sslmode=require
DB_URL=jdbc:postgresql://db.<project-ref>.supabase.co:5432/postgres?sslmode=require
DB_USERNAME=postgres
DB_PASSWORD=your-supabase-db-password

# ── JWT ──────────────────────────────────────────────────
# En az 64 karakter, rastgele üret:
#   openssl rand -base64 64
JWT_SECRET_KEY=replace-with-64-char-random-secret

# ── SOCIAL LOGIN (Google / Apple) ────────────────────────
# Google Cloud Console > OAuth 2.0 Client IDs
# Backend, idToken audience alanını bu değerlerle eşleştirir.
# Web/server client id zorunlu; iOS/Android client id'leri desteklenen platformlara göre ekleyin.
AUTH_GOOGLE_ALLOWED_CLIENT_IDS=your-google-web-client-id.apps.googleusercontent.com,your-google-ios-client-id.apps.googleusercontent.com

# Apple Developer > Identifiers
# iOS bundle id ve gerekiyorsa Android/web için Service ID'yi virgülle ayır.
AUTH_APPLE_ALLOWED_AUDIENCES=com.eneskotay.pusulafit

# ── AI KEYS ──────────────────────────────────────────────
GEMINI_API_KEY=your-gemini-api-key
CLAUDE_API_KEY=your-claude-api-key

# ── IN-APP PURCHASE ───────────────────────────────────────
# App Store Connect > My Apps > Subscriptions > Shared Secret
IAP_APPLE_SHARED_SECRET=your-apple-shared-secret
IAP_APPLE_BUNDLE_ID=com.eneskotay.pusulafit
IAP_APPLE_SANDBOX=false
IAP_VERIFY_MODE=strict

# Google Play Console > Setup > API Access > Service Account
# JSON dosyasını tek satıra: jq -c . service-account.json
IAP_GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
IAP_GOOGLE_PACKAGE_NAME=com.pusulafit.tracker

# ── EMAIL (Gmail App Password) ────────────────────────────
# Gmail > Google Account > Security > 2FA > App Passwords
MAIL_USERNAME=your-gmail@gmail.com
MAIL_PASSWORD=your-16-char-app-password
MAIL_FROM=noreply@pusulafit.com
MAIL_MOCK=false

# ── QUARKUS PROFILE ───────────────────────────────────────
QUARKUS_PROFILE=prod
